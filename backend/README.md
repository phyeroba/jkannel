# JKANNEL Backend

NestJS/TypeScript modular backend. The versioned health contract is `GET /api/v1/health`. Phase 2 supplies validated environment configuration, JSON logging, request/correlation IDs, response envelopes, and global error handling.

Run `npm ci`, `npm test`, `npm run test:integration`, and `npm run build` from this directory.
