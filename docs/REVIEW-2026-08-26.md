# Review list — 2026-08-26

Peter's walkthrough of the running console. Each item is verified in the browser
before it is called done, and struck through only when a measurement or a test
says so.

## Dialogs that are squashed
- [ ] **Carriers → Add carrier** — the popup is squashed. Same for Edit.
- [ ] **Recipient Policy → Add an entry** — squashed, badly organised.
- [ ] **Customers → Add customer** — needs reorganising.
- [ ] Go through every dialog in Playwright and look at what is actually
      rendered, rather than trusting the markup.

## Missing actions
- [ ] **SMSC Manager** — has Add, Test and Reconnect but no Edit and no Delete.

## Panes that need horizontal room or scrolling
- [ ] **Live Traffic → Throughput over time** — the right-hand end is cut off.
- [ ] **SMPP Errors → Throttling in context** — squashed, wants more width.
- [ ] **Performance → Capacity** — needs scrolling.

## Grids missing pagination / filtering / sorting
- [ ] **SMPP Errors → Command status reference** — no pagination, no filter.
- [ ] **Log Explainer → Entries** — no pagination, no sorting.
- [ ] **Messages** — no pagination.

## Layout
- [ ] **Log Explainer → Trace an incident** — cramped, needs reorganising.

## Errors on screen
- [ ] **System / Nodes** — "some node resource usage could not be loaded".
- [ ] **Reports** — "too many authentication attempts, try again later".
- [ ] **API Gateway** — the same authentication-attempts error.

## End-to-end behaviour to prove, not assume
- [ ] **Backup and restore** — no backup has ever actually been taken. Take one
      and see what it produces.
- [ ] **Content filtering** — prove a caught message is acted on, including
      block.
- [ ] **Blacklist / whitelist** — confirm whether the app has them at all.
- [ ] **Recipient Policy** — the whole workflow, end to end.
- [ ] **Inbound routing** — end to end.
- [ ] **Customers** — seed one through the UI.

## Noted, not a priority
- [ ] "What is not measured here" — add some of the missing measurements.
