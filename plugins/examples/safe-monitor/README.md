# Safe Monitor example

This is a manifest fixture, not a trusted production package. Its placeholder checksum and signature intentionally fail package-integrity validation when package bytes and an approved publisher key are supplied.

It requests one read-only permission, declares every event direction, and contains no engine-specific behavior.
