# JKANNEL Frontend

Vue 3/TypeScript/Vite/Tailwind application shell. Visual work must follow `design/design_spec/` and the canonical UI specification without copying JVIDEO terminology.

Run `npm install` and `npm run build` from this directory.

