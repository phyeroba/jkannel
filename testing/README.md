# Testing

Tests are organized by scope: `unit`, `integration`, `e2e`, `performance`, and `security`. Phase 1 includes a backend unit health-contract test; subsequent phases must add tests at the appropriate shared level.

